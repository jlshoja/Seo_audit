{
  "name": "seo-audit-pro",
  "version": "1.0.0",
  "description": "Integrated SEO Audit Tool - Technical Crawl + Core Web Vitals + Unified Reports",
  "private": true,
  "type": "module",
  "scripts": {
    "crawl": "python src/crawler/seo_audit.py --config config.json --cwd reports --output crawl_results.json",
    "speed": "node src/speed/seo-speed-audit.js --config config.json --output reports/speed_results.json",
    "report": "node src/report/merge-report.js --config config.json --crawl reports/crawl_results.json --speed reports/speed_results.json --output reports",
    "ai": "node src/ai/analyze.js --config config.json",
    "full": "npm run crawl && npm run speed && npm run report",
    "audit": "npm run crawl && npm run speed && npm run report && npm run ai",
    "doctor": "node scripts/doctor.mjs",
    "test": "node --check src/report/merge-report.js && node --check src/speed/seo-speed-audit.js && node --check src/ai/analyze.js && node --check scripts/doctor.mjs"
  },
  "dependencies": {
    "chart.js": "^4.4.4",
    "chrome-launcher": "^1.1.0",
    "lighthouse": "^13.4.1"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "npm@10.0.0"
}
